"""scheduler v0.1 — 定时任务模块（占位）。

本包对应 ``docs/agent-cron-module-v0.1/``。v0.1 暂只提供领域层：
- :mod:`scheduler.domain` — frozen dataclass / StrEnum / 常量真源

后续模块（store / trigger engine / execution bridge / safety bridge）按
``dev-pipeline/tasks/agent-cron-module-v0.1/dev-checklist.md`` 的顺序逐步加入。
"""
