# kongming-agent web v0.1.5 环境变量样例
#
# 复制为 .env 后修改；vite 自动读取 import.meta.env.VITE_*。

# REST API 基础前缀；dev 留空，走 vite proxy → localhost:8080
VITE_API_BASE=

# WebSocket 基础前缀；dev 留空，走 vite proxy + 同源 host
VITE_WS_BASE=
